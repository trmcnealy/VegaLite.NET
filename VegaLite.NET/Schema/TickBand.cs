namespace VegaLite.Schema
{
    /// <summary>
    /// For band scales, indicates if ticks and grid lines should be placed at the center of a
    /// band (default) or at the band extents to indicate intervals.
    /// </summary>
    public enum TickBand
    {
        Center,
        Extent
    };
}
